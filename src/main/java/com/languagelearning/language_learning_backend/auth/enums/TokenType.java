package com.languagelearning.language_learning_backend.auth.enums;

/** Loại VerificationToken — xác thực email khi đăng ký, hoặc đặt lại mật khẩu quên. */
public enum TokenType {
    EMAIL_VERIFY,
    PASSWORD_RESET
}
